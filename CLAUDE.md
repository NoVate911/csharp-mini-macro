# CLAUDE.md — Mini Macro

## О проекте

**Название:** Mini Macro  
**Автор:** NoVate Source  
**Стек:** C# (.NET 10.0 / .NET Framework 4.8), WPF, Visual Studio Community 2026  
**Формат файлов макросов:** `.mmacro` (собственный формат)

---

## Версии

| Версия  | Платформа              | ОС             |
|---------|------------------------|----------------|
| Modern  | .NET 10.0              | Windows 10/11  |
| Legacy  | .NET Framework 4.8     | Windows 7 SP1+ |

- Мультитаргетинг в одном `.csproj`: `<TargetFrameworks>net10.0-windows;net48</TargetFrameworks>`
- Символ условной компиляции для Legacy: `LEGACY`
- API-различия изолируются через `#if LEGACY ... #endif`
- Сборка конкретной версии: `dotnet build -f net10.0-windows` / `dotnet build -f net48`

---

## Системы

### 1. Запись действий
- Перехват событий клавиатуры и мыши через Windows API (hooks)
- Учёт разрешения экрана пользователя (координаты мыши нормализуются)
- Поддержка модификаторов: Shift, Ctrl, Alt, Win
- Поддержка сочетаний клавиш

### 2. Воспроизведение
- Точное воспроизведение записанных событий с учётом задержек
- Скорость регулируется (до 5x)
- Количество повторов (до 50)

### 3. Сохранение в файл `.mmacro`
- Собственный формат (JSON или бинарный — решить в ходе разработки)
- Расширение `.mmacro`

### 4. Загрузка из файла
- Открытие `.mmacro` из любой папки (стандартный диалог)

### 5. Библиотека макросов
- Пользователь указывает папку; все `.mmacro` из неё отображаются в интерфейсе
- Функции: назначение горячей клавиши, удаление, перезапись, загрузка

### 6. Меню настроек
- Слайдер: количество повторов (1–50)
- Слайдер: скорость воспроизведения (0.5x–5x)
- Кнопка смены папки библиотеки макросов

---

## Дизайн-система

| Параметр | Значение |
|---|---|
| Тема | Тёмная (с возможностью добавить светлую) |
| Акцентный цвет | `#1ED895` |
| Стиль | Минималистичный, компактный |
| Скругления | Минимальные, единообразные |
| Иконки | SVG через `Path` (без сторонних библиотек) |
| Анимации | Лёгкие, не ресурсозатратные |
| Размер окна | ~680×480 |
| Компоненты | Все стандартные (Button, ScrollBar, RadioButton и т.д.) — стилизованы |

Логотип: `logo.ico` (добавляется пользователем в корень проекта).

---

## Стратегия веток

- **`dev`** — вся активная разработка и тестирование
- **`main`** — только стабильный код для публичных релизов

Коммиты и PR: сначала в `dev`, после проверки — merge в `main`.

---

## Релизы (GitHub Releases)

При публикации релиза:
1. Собрать `.exe` (Release конфигурация, self-contained или framework-dependent — уточнить)
2. Написать описание: что изменено, ссылки на коммиты
3. Описание — на русском, структурированное

---

## Соглашения по коду

- **Язык:** везде русский — коммиты, комментарии, документация
- **Комментарии в коде:** только там, где логика неочевидна; не документировать тривиальное
- **Паттерн:** MVVM (предпочтительно)
- **Nullable:** включён (`<Nullable>enable</Nullable>`)
- **Implicit usings:** включены
- **Пространство имён:** `MiniMacro`
- **Имя сборки:** `MiniMacro`
